export default function Loading() {
    return (
      <div className="w-full flex-center flex-col">
        <h2 className="head_text text-center">Loading...</h2>
        <p className="desc text-center">Please wait while we fetch the latest prompts for you.</p>
      </div>
    )
  }